import React from 'react';
import styled from 'styled-components';

const SapperGame = ({ items }) => {
  // const itemsCount = 16;
  // const itemsSize = () => {
  //   return 
  // }

  const Canvas = styled.canvas`
    width: 50vh;
    height: 50vh;
    background: #f08f6c;
  `

  return (
    <Canvas />
  )
};

export default SapperGame;